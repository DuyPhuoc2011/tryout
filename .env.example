# API
PORT=3001
# Host ports 5542 / 6479 match docker-compose.yml (chosen to avoid local
# services already bound to the default 5432 / 6379).
DATABASE_URL=postgres://tryout:tryout@localhost:5542/tryout
REDIS_URL=redis://localhost:6479
JWT_SECRET=dev-only-change-me
JWT_EXPIRES_IN=7d

# Web
NEXT_PUBLIC_API_URL=http://localhost:3001
